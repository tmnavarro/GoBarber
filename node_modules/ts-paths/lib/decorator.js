"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
const metadata_1 = require("./metadata");
exports.Value = (value = "") => {
    return (target, propertyKey) => {
        const descriptor = Reflect.getOwnPropertyDescriptor(target, propertyKey) || {
            writable: true,
            configurable: true
        };
        descriptor.value = value;
        Reflect.defineProperty((target && target.prototype) || target, propertyKey, descriptor);
    };
};
exports.Autowired = (params = "") => {
    return (target, propertyKey) => {
        let typeClass = metadata_1.Metadata.getType(target, propertyKey);
        const descriptor = Reflect.getOwnPropertyDescriptor((target && target.prototype) || target, propertyKey) || {
            writable: true,
            configurable: true
        };
        descriptor.value = params ? new typeClass(params) : new typeClass();
        Reflect.defineProperty((target && target.prototype) || target, propertyKey, descriptor);
    };
};
//# sourceMappingURL=decorator.js.map